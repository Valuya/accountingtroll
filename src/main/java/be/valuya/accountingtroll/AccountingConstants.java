package be.valuya.accountingtroll;

import java.math.BigDecimal;
import java.math.RoundingMode;

public class AccountingConstants {
    public static final BigDecimal AMOUNT_ZERO = BigDecimal.ZERO.setScale(3, RoundingMode.UNNECESSARY);

}
